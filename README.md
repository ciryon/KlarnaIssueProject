## This projects aims to reproduce problems integrating the Klarna-on-Demand CocoaPod.

The Pods directory is included so no need to run pod install.

Just open the KlarnaIssueProject.xcworkspace and build.

